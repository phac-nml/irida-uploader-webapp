# Use debug outputting
DEBUG = True

# Run directory to have the application monitor
# directory must end with / character
MONITOR_DIRECTORY = "examples/"

# iridauploader config file to use for IRIDA connection
CONFIG_FILE = "examples/example_config.conf"
